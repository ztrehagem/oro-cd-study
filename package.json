{
  "name": "oro-cd-study",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "parcel src/index.pug",
    "build": "npm run clean && parcel build --public-url . --no-source-maps src/index.pug",
    "clean": "rm -r dist || true",
    "clean:cache": "rm -r .cache || true",
    "release": "git subtree push --prefix dist origin master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vue/component-compiler-utils": "^2.6.0",
    "parcel-bundler": "^1.11.0",
    "pug": "^2.0.3",
    "stylus": "^0.54.5",
    "typescript": "^3.3.3333",
    "vue": "^2.6.7",
    "vue-template-compiler": "^2.6.7"
  },
  "dependencies": {
    "vue-hot-reload-api": "^2.3.3"
  }
}
